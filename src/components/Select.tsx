export interface SelectPropsType {
  item: [string | number];
  value?: string | number;
  func?: (value: string | number) => void;
}

const Select = (props: SelectPropsType) => {
  // const { item, value, func } = props;

  return <div></div>;
};

export default Select;
